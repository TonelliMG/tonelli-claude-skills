# AI assistants — ignore agent/config artifacts, but keep CLAUDE.md versioned

# Claude
.claude/
.claude.json
.claude.json.backup

# Codex
.codex/
CODEX.md

# Other agents (Cursor, Aider, Gemini, Copilot, Windsurf, etc.)
.cursor/
.cursorrules
.aider*
.gemini/
GEMINI.md
.windsurf/
.github/copilot-instructions.md

# Keep CLAUDE.md and AGENTS.md tracked even though .claude/ and .agents/ are ignored
!CLAUDE.md
!AGENTS.md

# Flutter — generated artifacts (e.g. if a skill is tested locally)
.dart_tool/
.flutter-plugins
.flutter-plugins-dependencies

# OS / editor noise
Thumbs.db
.DS_Store
.vscode/
.idea/
